# E2 run findings — 2026-08-10

Run: `docs/2026-08-10-e2-live-reproduction-runbook.md`, executed owner + assistant, ~10:34-10:56 RDT.

## Phase A — Telegram tap reproduction (GL-45 test half)

Tap: group 76 (candidate 49, 5x7), digest message 286, `approve:76`.

**Result: full pass, all conditions met.**

- Raw log (`logs\telegram_getupdates.log`) captured `update_id 475586414`,
  `callback_query` `approve:76`, message 286 — matches tap.
- `telegram_events_log` id 55: `accepted=1, action_taken='approve'`.
- `groups.id=76`: `decision='approved'`.
- `heartbeats`: fresh `hourly` row in `db/qhoto.sqlite3` (repo root) —
  discharges GL-38 Phase D step 13.
- Re-tap guard exercised: second tap on the collapsed `✅ Approved` label
  produced `telegram_events_log` id 56, `accepted=0,
  action_taken='ignored: already decided'`; `groups.id=76` unchanged.
- No update_id gap (414 → 415, sequential).

**Reading (per runbook §0 table): row 1** — the path works, tap was not
dropped. **Root cause of the 08-09 loss remains unproven** — it did not
recur here. GL-45's row should be ticked as tested, with this stated plainly:
a clean run, not a diagnosis.

One process note, not a defect: toast and keyboard-collapse (`editMessageReplyMarkup`)
both land only when the hourly poll dispatches the update, not at tap time —
expected given the bot has no persistent listener. Worth being explicit about
this in GL-45's row so a future reader doesn't mistake the delay for a bug.

## Phase B — live Gelato + Etsy create (GL-48 §7)

Candidate 49, three groups complete (53 primary approved pre-existing, 76 and
77 approved this run). Live create fired on the next hourly run.

**Result: pass on the specific claim tested, new defect found on manual inspection.**

- Exactly one Gelato product created for candidate 49:
  `gelato_product_id=3e7abdce-c055-4609-ae68-aab19868c5a0`,
  `etsy_listing_id=4553104678` (create-once held).
- `scripts/gelato_template_check.py` (run with `PYTHONIOENCODING=utf-8` —
  the script crashes on the `″` character under the default Windows cp1252
  console encoding when printing later variants; a pre-existing tooling bug,
  not part of this finding) measured 10x24 placed artwork aspect at
  **0.4176** — matches the expected ~0.42, not the pre-fix 0.65. **GL-48's
  fix is confirmed live by measurement**, closing that open claim.
- Six `group_product_variants` rows for the product, prices match v4.11 §4
  exactly: 5x7 €19, 8x12 €24, A3 €35, A2 €39, 10x24 €45, A1 €49.
- Candidate 42's listing (`4549960823`, GL-36's negative control) confirmed
  untouched.
- Both live-mode flags reset to `FALSE` after the run.

### New defect (not GL-48, not previously characterised)

Manual inspection of the created product in Gelato's Design editor: the
**10x24 vertical variant's artwork is cropped past the frame edge** — top of
the flower and bottom of the stem are both cut off outside the printable
area, rather than sitting inside it. Screenshot on file (owner's
2026-08-10 message).

This is **not** a recurrence of the GL-48 placement-rect defect — the
measured placed-artwork aspect for this exact variant is 0.4176, correct.
The rect the artwork is placed into is right; the **crop applied to the
artwork within that rect** is wrong — the image reads as over-zoomed,
losing content at both ends. Different mechanism, same size class (10x24),
same live product. Root cause not investigated in this session — this run
is manual-observation only per its own scope ("Type: M ... No code is
written here").

Needs a name on the board (working label GL-51) and a proper root-cause
session before any fix.

## Concurrent second-consumer check (GL-46/47 session request)

GL-46/47 coding session reported near-continuous pytest runs, ~1000+
throwaway DBs, wall-clock 08:38-08:55 UTC (10:38-10:55 local RDT,
local = UTC+2, confirmed via `date -u`).

E2's three real hourly-run log entries fall inside that window:

| local | UTC | offset | update_id | content |
|---|---|---|---|---|
| 10:41:01 | 08:41:01 | 475586405 | 475586414 | `approve:76` |
| 10:43:07 | 08:43:07 | 475586415 | 475586415 | `noop:76` |
| 10:46:25 | 08:46:25 | 475586416 | 475586416 | `approve:77` |

**Clean.** Sequential offsets/update_ids, no gaps, no duplicates, no
foreign or unexplained entries in `logs\telegram_getupdates.log` for this
window, despite the concurrent throwaway-DB churn. This is affirmative
evidence the `sha256(bot_token)` lock and the `db_identity` guard hold
under concurrent-DB pressure — the leading second-consumer hypothesis for
the 08-09 loss did not manifest here.

## Scheduled tasks — post-run state

`qhoto-hourly` set back to **Disabled** (was enabled only for this run's
duration, per runbook §4.3). Both batch tasks confirmed still Disabled
throughout.
