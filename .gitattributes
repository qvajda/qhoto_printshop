# Normalize line endings: authoritative LF in the repo, autocrlf-friendly on checkout.
* text=auto eol=lf

# Binary assets — never touch EOL / never diff as text.
*.png binary
*.jpg binary
*.jpeg binary
*.sqlite3 binary
*.bak binary
*.csv text eol=lf
