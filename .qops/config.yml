# qops project configuration — TRACKED (PRD v3 decision 21).
# Everything else qops writes under .qops/ is machine state and is ignored.
#
# This file is the ONLY place a project specific may live. The CLI, the hooks
# and the rendered workflows read it; none of them carries a project name, a
# path, an interpreter or a threshold of its own. Phase 7's portability proof
# is exactly "does anything project-specific exist outside this file".
#
# This file is also the importer's source for the label taxonomy. PRD v3 §9 is
# the prose version of the same thing; if the two disagree, this file is what
# runs.

project: qhoto_printshop
prd: docs/2026-08-13-qops-prd-v3.md
repo: qvajda/qhoto_printshop
default_branch: master

# The interpreter the hooks and the shims invoke. `python3` on this machine is
# the Windows Store stub, and a hardcoded absolute path is what E14 refused to
# commit — the py launcher is the portable middle.
python: py -3
python_posix: python3

# Whether this project keeps a picker on the host at all. `qops install`
# registers `pickup-loop`'s scheduled task from this config (ADR-0032); set
# false and no install anywhere touches the machine's scheduler.
pickup_task: true

# Whether the registered pickup task passes `--launch`. Default off: the
# flagless form prints what it would have picked and spends nothing.
pickup_launch: false

# How long the brief stays quiet about `pickup-loop` before it says the loop
# has not completed a run. A repo whose loop has never run is silent, because
# it ships disabled and never nagging about it is the point.
pickup_max_silence_hours: 3

# --- guard -----------------------------------------------------------------
protected_branches: [master]
max_worktrees: 2

# Project tripwires. Each is a regex plus the constraint it defends; the guard
# blocks the tool call locally and guard.yml fails the build in CI. Both read
# this list — one definition, two enforcement points.
tripwires:
  - name: create_draft_listing
    # The call, not the definition and not the word. etsy_client.py may define
    # it (it stays DELIBERATELY UNWIRED); the pipeline may never call it.
    pattern: '(?<!def )\bcreate_draft_listing\s*\('
    paths: ['pipeline/']
    why: 'Gelato pushes the Etsy listing and we patch it. Creating one collides with that push (CLAUDE.md, v4.11).'
  - name: flux_dev
    pattern: 'FLUX\.1 ?\[dev\]'
    # ASCII only: this string is printed to a Windows console by the hook.
    why: 'Artwork generation is FLUX.1 [schnell] only - the licence governs the artefact a buyer pays for.'
  - name: placeholder_template_id
    pattern: 'PLACEHOLDER[A-Z_]*'
    paths: ['config/static_config.json']
    why: 'A placeholder Gelato template id must never reach a real create-from-template call.'

# Files that name the tripwires on purpose: the constraint docs, the config,
# the guard itself and its tests. Excluded from `qops guard scan`, never from
# the PreToolUse hook.
scan_exclude:
  - CLAUDE.md
  - CONTEXT.md
  - README.md
  - docs/
  - .qops/
  - tests/test_qops_project.py   # this repo's tripwire assertions name them
  - .github/workflows/guard.yml   # the workflow that runs the scan names them

# --- skills (ADR-0018) -----------------------------------------------------
# The DECLARED set. ADR-0013 named the count as its mitigation and asked a human
# to re-read it; nobody did, and the installed set reached 19 against 11
# accepted. So the count is a check: `qops doctor` asserts that .claude/skills/
# holds exactly these and nothing else.
#
# native   — ours to maintain, tracked in git, they read this file.
# external — reinstallable copies; every one needs a `ref` in skills-lock.json,
#            because drift that cannot be observed is the actual defect.
skills:
  native: [interview, spec-to-issue, triage, pending]
  external: [build-models, compare-models, find-models, prompt-images,
             prompt-videos, publish-models, run-models]

# --- hot path --------------------------------------------------------------
# S6/S10. Enforced by groom.yml and by tests/test_qops.py, not by intention:
# CLAUDE.md grew +10 lines/day while the cap was only written down.
claude_md_max_lines: 150

# guard.yml's doc-link check: every docs/*.md path cited from these trees must
# resolve. Phase 3 broke 13 of 15 by archiving docs; only a check caught it.
doc_link_roots: [pipeline, scripts, tests]

# --- portability (Phase 8 P8.1) --------------------------------------------
# The word list `tests/test_qops.py` asserts against: no substrate source file
# may contain any of these, case-insensitively. This file may, because this
# file IS the project-specific surface. The project name and every tripwire
# name are added automatically; these are the vendors and models the audit had
# to grep for by hand three times.
#
# `telegram` is deliberately absent: digest.yml posts to a QOPS_TELEGRAM_DEV_*
# bot, which is the substrate's own notification channel and not the shop's.
portability_forbidden: [qhoto, printshop, etsy, gelato, replicate, flux,
                        schnell, mockup, listing]

# --- schema check (#160) ---------------------------------------------------
# `qops doctor` compares the live DB against the declared schema: schema.sql is
# all `CREATE TABLE IF NOT EXISTS`, so a column added there is a silent no-op
# against a table that already exists. Both paths are THIS project's — a config
# omitting this block gets no check, which is what a substrate repo wants.
schema_check:
  sql: db/schema.sql
  db: db/qhoto.sqlite3

# --- CI --------------------------------------------------------------------
ci:
  python_version: '3.12'
  test_command: python -m pytest -q
  # test_qops.py went with the substrate (ADR-0023). What is left here is
  # this project's half: its tripwires and its declared schema.
  gate_command: python -m pytest -q tests/test_qops_project.py
  runs_on: ubuntu-latest
  digest_cron: '0 6 * * *'      # 06:00 UTC daily
  digest_posts_on_schedule: true
  groom_cron: '0 5 * * 1'       # Mondays
  status_issue_label: 'qops:status'

# --- agents (§3.3 roster, §3.4 levers) -------------------------------------
# effort: start high, drop where quality holds, xhigh only for demanding
# agentic work. This is the project's only real cost control.
agents:
  planner:
    model: opus
    effort: high
    tools: [Read, Grep, Glob, Bash, WebFetch]
    # ADR-0033 class 3: a planner that edits the tree is a coder; the plan
    # goes onto the row through `gh`. Belt-and-braces alongside `tools`
    # already omitting Write/Edit.
    deny: [Write, Edit, MultiEdit, NotebookEdit]
  coder:
    model: sonnet
    effort: medium
    tools: [Read, Edit, Write, Grep, Glob, Bash]
  reviewer:
    model: sonnet
    effort: high
    tools: [Read, Grep, Glob, Bash]
  scribe:
    model: haiku
    effort: low
    tools: [Read, Edit, Write, Bash]
  triager:
    model: sonnet
    effort: low
    tools: [Bash, Read, Grep]
  interactor:
    model: haiku
    effort: low
    tools: [Bash, Read]

# --- taxonomy (PRD v3 §9) --------------------------------------------------
labels:
  type: [epic, research, code, manual, test, decision]
  state: [triage, planned, building, gate, review, blocked, done, cancelled]
  mission: [go-live, post-launch, housekeeping, qops]
  gate: [machine, taste, none]
  # ADR-0023, amended by ADR-0029: whose licence covers the row, not who typed
  # it — added here to match the substrate's current schema (#178: this repo's
  # taxonomy had drifted silently behind qops's own since the v0.1.1 pin).
  origin: [owner, agent, pending]
  # Optional and zero-or-one, deliberately NOT in `require_on_open`: an
  # unlabelled row is normal priority. Added for the same reason as `origin`.
  priority: [parked]
  # `qops:status` is the pinned digest issue's label (ci.status_issue_label).
  # It lived only in `ci:` until 2026-08-18, so the importer never created it
  # and digest.yml failed at 06:00 UTC every day for weeks (#136). Every
  # label named anywhere in this file must appear here; `qops doctor` asserts it.
  flags: [ready:auto, no-auto, fork, budget:approved, go-live-blocker, qops:status]

milestones: [go-live, post-launch, housekeeping, qops]

# Every OPEN issue must carry exactly one type:, one state:, one gate: and one
# origin:. gate:none is legal at import (review finding B7) and blocks
# ready:auto until it is filled in when the sortie is planned.
validate:
  require_on_open: [type, state, gate, origin]
  forbid_at_import: [ready:auto]
